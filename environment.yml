name: dicomviewer
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.14
  - numpy
  - pydicom
  - pyqt
  - sqlalchemy
  - pip
  - pip:
      - psycopg2-binary
      - pylibjpeg
      - pylibjpeg-libjpeg
      - pylibjpeg-openjpeg
